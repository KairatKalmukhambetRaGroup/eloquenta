/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package eloquenta.app;

import eloquenta.list.LinkedList;

import static eloquenta.utilities.StringUtils.join;
import static eloquenta.utilities.StringUtils.split;
import static eloquenta.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
